spring:
  application:
    name: central-doc-eureka-server
server:
  port: 9092
logging:
  level:
    com.netflix.eureka: false
    com.netflix.discovery: false
    
    
eureka:
  client:
    fetch-registry: false
    register-with-eureka: false